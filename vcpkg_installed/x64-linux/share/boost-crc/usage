The package boost-crc is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS crc)
    target_link_libraries(main PRIVATE Boost::crc)

or the generated cmake configs via:

    find_package(boost_crc REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::crc)
